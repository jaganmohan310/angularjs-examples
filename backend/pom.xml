<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>com.hms</groupId>
		<artifactId>hospital</artifactId>
		<version>0.0.1-SNAPSHOT</version>
		<relativePath>../pom.xml</relativePath>
	</parent>
	<artifactId>hms-backend</artifactId>
	<packaging>pom</packaging>
	<name>hms-backend</name>
	<description>Parent project for HMS Multi-Module Backend</description>

	<modules>
		<module>hms-common</module>
		<module>hms-dao</module>
		<module>hms-service</module>
		<module>hms-config</module>
		<module>hms-ws</module>
		<module>hms-oas</module>
		<module>hms-dist</module>
	</modules>

	<properties>
		<java.version>17</java.version>
	</properties>

	<dependencyManagement>
		<dependencies>
			<dependency>
				<groupId>com.hms</groupId>
				<artifactId>hms-common</artifactId>
				<version>${project.version}</version>
			</dependency>
			<dependency>
				<groupId>com.hms</groupId>
				<artifactId>hms-dao</artifactId>
				<version>${project.version}</version>
			</dependency>
			<dependency>
				<groupId>com.hms</groupId>
				<artifactId>hms-service</artifactId>
				<version>${project.version}</version>
			</dependency>
			<dependency>
				<groupId>com.hms</groupId>
				<artifactId>hms-config</artifactId>
				<version>${project.version}</version>
			</dependency>
			<dependency>
				<groupId>com.hms</groupId>
				<artifactId>hms-ws</artifactId>
				<version>${project.version}</version>
			</dependency>
			<dependency>
				<groupId>com.hms</groupId>
				<artifactId>hms-oas</artifactId>
				<version>${project.version}</version>
			</dependency>
		</dependencies>
	</dependencyManagement>

	<build>
		<plugins>
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
				<configuration>
					<skip>true</skip>
				</configuration>
			</plugin>
		</plugins>
	</build>

</project>
