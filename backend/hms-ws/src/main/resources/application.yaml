server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/hmsdb
    username: postgres
    password: Dhanu123  # <-- your real password
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect

