import { Component } from '@angular/core';
import { RouterOutlet } from '@angular/router';

@Component({
  selector: 'app-root',
  standalone: false,
  template: '<router-outlet />',
  styles: []
})
export class App {
}
